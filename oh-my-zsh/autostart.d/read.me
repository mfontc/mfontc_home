#!/bin/zsh
# vim:ft=zsh ts=4 sw=4 sts=4 noexpandtab

cat <<README-ME

cat > keychain.sh <<KEYCHAIN-SH
# vim:ft=zsh ts=4 sw=4 sts=4 noexpandtab
fpath=( ~/.mfontc_home/oh-my-zsh/zfuncs "\\\${fpath[@]}" ) && autoload -Uz mfontc-keys && mfontc-keys && mfontc-keys-loadq
KEYCHAIN-SH
chmod u+x keychain.sh

cat > default_user.sh <<DEFAULT_USER-SH
# vim:ft=zsh ts=4 sw=4 sts=4 noexpandtab
DEFAULT_USER=\\\${USER}
DEFAULT_USER-SH
chmod u+x default_user.sh

README-ME

